import fs from "fs"; import * as cheerio from "cheerio"; const html = fs.readFileSync("rozee_test_pptr.html", "utf8"); const $ = cheerio.load(html); $(".job").each((i, el) => { const title = $(el).find("h3 a").text().trim(); const url = $(el).find("h3 a").attr("href"); const company = $(el).find("a[href*=\"/company/\"], a[href*=\"/employer/\"]").text().trim(); console.log(`Job ${i}: title="${title}", url="${url}", company="${company}"`); });
