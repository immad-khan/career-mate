import fs from "fs"; import * as cheerio from "cheerio"; const html = fs.readFileSync("rozee_test_pptr.html", "utf8"); const $ = cheerio.load(html); console.log("count of h3:", $("h3").length); console.log("h3 > a:", $("h3 > a").length); console.log("div.job:", $("div.job").length); const jobs=[]; $("div.job").each((_,e)=>jobs.push($(e).text().substring(0,20).replace(/\s+/g," "))); console.log(jobs.length>0?jobs:"No job divs found");
