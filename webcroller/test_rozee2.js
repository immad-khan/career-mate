import fs from "fs"; import * as cheerio from "cheerio"; const html = fs.readFileSync("rozee_test.html", "utf8"); const $ = cheerio.load(html); console.log("count of h3:", $("h3").length); console.log("h2:", $("h2").length); console.log("count of .job:", $(".job").length); const jobs = []; $(".job").each((i, el) => jobs.push($(el).text().substring(0,50).trim().replace(/\s+/g," "))); console.log("Jobs:", jobs);
