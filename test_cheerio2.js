import fs from "fs"; import * as cheerio from "cheerio"; const html = fs.readFileSync("rozee_test_pptr.html", "utf8"); const $ = cheerio.load(html); $(".job").each((i, el) => { const title = $(el).find("h3 a, h2 a, a.title, .job-title a, h3.s-18 a").text().trim(); const url = $(el).find("h3 a, h2 a, a.title, .job-title a, h3.s-18 a").attr("href"); console.log(`Job ${i}: title="${title}", url="${url}"`); });
